// **** This is what we are going to get to get the strategy from the vault and then we can use the strategy for all of the fancy data

import { loadEthers } from "../utils/ethers";

function getStrategyAPY(strategy: string) {
    const ethers = loadEthers();
}
